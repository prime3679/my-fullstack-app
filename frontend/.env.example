# La Carta Frontend Environment Variables

# This file contains example environment variables for the La Carta frontend.
# Copy this file to .env.local and fill in your actual values.

# ============================================================================
# API Configuration
# ============================================================================

# Backend API URL
# Development: http://localhost:3001/api/v1
# Production: https://api.yourdomain.com/api/v1
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# ============================================================================
# Stripe Payment Integration
# ============================================================================

# Stripe Publishable Key (Public - safe to expose in frontend)
# Get this from: https://dashboard.stripe.com/apikeys
#
# Test Mode (starts with pk_test_):
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51234567890abcdefghijklmnopqrstuvwxyz
#
# Live Mode (starts with pk_live_):
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_51234567890abcdefghijklmnopqrstuvwxyz
#
# IMPORTANT:
# - Use TEST keys for development (pk_test_)
# - Use LIVE keys only in production (pk_live_)
# - This key is PUBLIC and embedded in your frontend JavaScript
# - Never put your SECRET key (sk_) in frontend code!

# ============================================================================
# Application Configuration
# ============================================================================

# Application Environment
# Options: development, staging, production
NEXT_PUBLIC_APP_ENV=development

# Application URL (for OAuth redirects, social sharing, etc.)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# Optional: Analytics & Monitoring (Future)
# ============================================================================

# Google Analytics ID (if using)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking (if using)
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ============================================================================
# Development & Debugging
# ============================================================================

# Enable debug logging in development
NEXT_PUBLIC_DEBUG=false

# ============================================================================
# Setup Instructions
# ============================================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Get your Stripe keys:
#    - Login to https://dashboard.stripe.com
#    - Go to Developers > API keys
#    - Copy your Publishable key (starts with pk_test_ or pk_live_)
#    - Paste it into NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY above
#
# 3. Update API URL if different:
#    - For local development: http://localhost:3001/api/v1
#    - For production: Your production API URL
#
# 4. Restart your Next.js dev server after changing .env.local:
#    npm run dev
#
# Note: .env.local is gitignored and should never be committed to git!

# ============================================================================
# Security Notes
# ============================================================================

# ✅ SAFE to put in frontend (NEXT_PUBLIC_ prefix):
#    - Stripe Publishable Keys (pk_test_ or pk_live_)
#    - Public API URLs
#    - Analytics IDs
#    - Public configuration
#
# ❌ NEVER put in frontend:
#    - Stripe Secret Keys (sk_test_ or sk_live_)
#    - Database credentials
#    - Private API keys
#    - Webhook secrets
#    - JWT secrets
#
# These should only be in backend .env file!
